"""Внешние источники каталожных номеров.

Каждый парсер — модуль с функцией fetch(session, ctx) -> list[Found].
Ядро подсказки (app/services/oem.py) не знает, откуда берутся номера:
источник добавляется строкой в SOURCES и общим весом, а правило
«голоса одной family не складываются» защищает от того, что пять
магазинов процитируют один TecDoc и создадут ложную уверенность.
"""

from . import search

__all__ = ["search"]
